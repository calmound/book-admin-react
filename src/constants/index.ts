export enum USER_ROLE {
  ADMIN = "admin",
  USER = "user",
}

export enum USER_STATUS {
  ON = "on",
  OFF = "off",
}

export enum USER_SEX {
  MALE = "male",
  FEMALE = "female",
}

export enum BORROW_STATUS {
  ON = "on",
  OFF = "off",
}
